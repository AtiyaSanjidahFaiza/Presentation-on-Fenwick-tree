# Presentation-on-Fenwick-tree
A Fenwick Tree, or Binary Indexed Tree (BIT), is a data structure used for efficient prefix sum and update operations on arrays. It performs both operations in O(log n) time using binary index manipulation. It is commonly used in competitive programming and range sum query problems.
